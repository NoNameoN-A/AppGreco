<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.grecosapp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/avatar"
        android:label="@string/app_name"
        android:roundIcon="@drawable/avatar"
        android:supportsRtl="true"
        android:theme="@style/Theme.GrecosApp">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <activity android:name=".activity_manager.GoogleMaps" />




        <activity android:name=".activity_manager.CheckBOTActivity" />
        <activity android:name=".activity_manager.CheckBlacklistActivity" />
        <activity android:name=".activity_manager.ISPCheckActivity" />
        <activity android:name=".activity_manager.TorCheckActivity" />
        <activity android:name=".activity_manager.ProxyCheckActivity" />
        <activity android:name=".activity_manager.VPNCheckActivity" />
        <activity android:name=".activity_manager.WhoisIPActivity" />
        <activity android:name=".activity_manager.ShareActivity" />
        <activity android:name=".activity_manager.RegisterActivity" />
        <activity android:name=".activity_manager.RateActivity" />
        <activity android:name=".activity_manager.ProfileActivity" />
        <activity android:name=".activity_manager.LogoutActivity" />
        <activity android:name=".activity_manager.LoginActivity" />
        <activity android:name=".activity_manager.HomeActivity" />
        <activity android:name=".activity_manager.CreditsActivity" />
        <activity android:name=".activity_manager.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>